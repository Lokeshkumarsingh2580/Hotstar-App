import React from "react";

const Loading = () => {
  return (
    <div className="h-screen w-full bg-zinc-900 text-white text-4xl flex items-center justify-center">
      <h1>Please Wait While It's Loading</h1>
    </div>
  );
};

export default Loading;
